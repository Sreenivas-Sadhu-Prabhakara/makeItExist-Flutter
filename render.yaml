# render.yaml — Render Blueprint for one-click deploy
# https://render.com/docs/blueprint-spec

databases:
  - name: makeitexist-db
    plan: free
    databaseName: makeitexist
    user: makeitexist

services:
  - type: web
    name: makeitexist
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: SERVER_PORT
        value: "8080"
      - key: SERVER_ENV
        value: production
      - key: FRONTEND_DIR
        value: /app/static
      - key: DATABASE_URL
        fromDatabase:
          name: makeitexist-db
          property: connectionURI
      - key: DB_SSLMODE
        value: require
      - key: JWT_SECRET
        generateValue: true
      - key: AIM_EMAIL_DOMAIN
        value: aim.edu
      - key: CORS_ALLOWED_ORIGINS
        value: "*"
      - key: GOOGLE_AUTH_CLIENT_ID
        sync: false
